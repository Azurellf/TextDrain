This directory contains small fixtures used by automated tests.

- `media/sample-audio.wav` is a minimal audio placeholder for pipeline tests that mock media tools.
- `media/sample-video.mp4` is a minimal video placeholder for pipeline tests that mock media tools.
- `commands/*.json` stores representative command outputs from external tools so tests can run without network access or real binaries.
